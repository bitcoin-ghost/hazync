# Live name: /etc/systemd/system/<unit>.service.d/alert.conf — push to a phone when this unit fails.
# A failed oneshot is otherwise a journal line nobody reads (2026-09-14: the G1 check failed nightly unnoticed).
[Unit]
OnFailure=hazync-alert@%n.service

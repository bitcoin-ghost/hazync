.DS_Store
target/
__pycache__/
*.pyc
# Cargo.lock is COMMITTED (not ignored): the host/guest are binaries whose reproducible
# build — and thus the guest image id (METHOD_ID) — requires a frozen dependency set.
